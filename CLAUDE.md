# Project Instructions

- After making code changes, always `git add`, `git commit`, and `git push` to `origin main` automatically — no need to ask for confirmation each time. This repo is connected to Vercel, which auto-deploys on push to `main`.
